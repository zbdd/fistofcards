<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Prepare new conduit
status = "sleep";
enemies = ds_list_create(); // 0 refers to selected enemy.
pool = ds_list_create(); // Available cards.
deck = ds_list_create(); // Available cards left.
chains = ds_list_create(); // Chained cards.
active = ds_list_create(); // Active cards to display and choose from.
discard = ds_list_create(); // Discarded cards from the deck.

// Prepare new chain
var main_chain = instance_create(x,y,o_chain);
ds_list_add(chains, main_chain);
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///State progression

pressed = 0;
// Hard set for now...
HAND_SIZE = 3;
if (HAND_SIZE &gt; ds_list_size(deck)) HAND_SIZE = ds_list_size(deck); 

if (status == 'listen') {
    // Listen for keys corresponding to active cards.
    for (i = 1; i &lt; ds_list_size(active) + 1; i++) {
        if (keyboard_check_pressed(ord(string(i)))) {
            pressed = 1 * i;
        }
    }
    
    // Something was pressed.
    if (pressed &gt; 0) {
        status = 'action';
    }
} else if (status == 'action') {
    status = 'discard';

    // Add a copy to the chain. (The original chain)
    // More chains &gt; more attacks.
    show_debug_message(object_get_name(active[| pressed].object_index));
    show_debug_message("*********************");
    ds_list_add(chains[| 0].chain, instance_create(x,y,active[| pressed].object_index));
    
    // Add more cards?
    
    // Reshuffle everything?
    
    // etc...
} else if (status == 'start') {
    status = 'listen';
    
    ds_list_copy(deck, pool);
    ds_list_shuffle(deck); // Forced shuffle.
    
    // Add starter cards to 'active' pool in start state.
    for (i = 0; i &lt; ds_list_size(deck); i++) {
        if (deck[| i].type == 'starter') {
            ds_list_add(active, deck[| i]);
            ds_list_delete(deck, i);
        }
    }
} else if (status == 'discard') {
    status = 'shuffle';
    
    for (i = 0; i &lt; ds_list_size(active); i++) {
        ds_list_add(discard, active[| i]);
    }
    ds_list_clear(active);

} else if (status == 'shuffle') {
    status = 'deal';
    
    ds_list_shuffle(deck);
} else if (status == 'deal') {
    status = 'listen';
    
    for (i = 0; i &lt; HAND_SIZE; i++) {
        // Forced shuffle and deal when the deck is all used up.
        if (ds_list_size(deck &lt;= 0)) {
            ds_list_copy(deck, discard);
            ds_list_shuffle(deck);
        }
        if (deck[| i].type == 'chain') {
            ds_list_add(active, deck[| i]);
            ds_list_delete(deck, i);
        }    
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="8" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Draw the HUD
// Simple setup.
x = view_xview[0];
y = view_yview[0];
w = view_wview[0];
h = view_hview[0];

// Main HUD background.
draw_set_color(c_gray);
draw_rectangle(x,y,x + w,y + h/4,false);

// Default boxes for cards.
for (index = 0; index &lt; ds_list_size(active); index++) {
    draw_set_color(c_dkgray);
    h_bg = draw_rectangle(x + (w / 16 * (index * 4 + 1)), y + (h / 24 * 1),
        x + (w / 16 * (index * 4 + 3)), y + (h / 24 * 5), false); 
    
    // Draw the actual cards.
    draw_set_font(fn_system);
    for (i = 0; i &lt; ds_list_size(active); i++) {
        active[| i].x = x + (w / 16 * (index * 4 + 1));
        active[| i].y = y + (h / 24 * 1);
        with (active[| i]) {
            draw_self();
        }
    }
}

</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
